interface ForgeOptions {
  usePureJavaScript?: boolean
}

/**
 * The Forge module.
 */
export const options: ForgeOptions = {
  usePureJavaScript: false,
}

// Default export (for backwards compatibility)
export default options
